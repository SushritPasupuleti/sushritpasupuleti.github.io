[project]
name = "audio-blogs"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "ollama>=0.3",
    "kokoro>=0.9",
    "huggingface-hub>=0.23",
    "soundfile",
    "numpy",
    "pyyaml",
    "tqdm",
]

[project.scripts]
generate-audio-blogs = "generate_audio_blogs:main"
